/**
 * Injection token for the McpModule options.
 */
export const MCP_MODULE_OPTIONS = "MCP_MODULE_OPTIONS";

/**
 * Injection token for the McpServer instance provided by the SDK.
 * While McpService wraps it, providing the raw server might be useful.
 */
export const MCP_SERVER_INSTANCE = "MCP_SERVER_INSTANCE";
